[x] a small semi-transparent color cube in in the corner that shows camera's current position as a dot,
acting as a minimap. Also show camera's cone of vision.
[x] while mouse dragging, don't move the cursor, so it's in the same position after releasing the mouse button
[x] HUD improvements:
  - in the top left box:
    - don't show frame ms, only FPS
    - remove second line
    - remove "camera" from the third line
    - add a speedometer similar to one in a car
  - in minimap reduce the margin around the cube, and remove the text below
  - add a "?" icon below the settings icon, that will toggle the hint box
[ ] add an option to see a HSL / HSV cylinder instead of RGB cube (see hsl_hsv.png).
  Add a color model select control in top left - to the left of the settings button.
[ ] Remove axes from the main cube. Keep the cube outline, but make it a bit thicker and black.
[ ] Add tests for existing features
[ ] In the top left box move the color swatch to the left, and add hex color display, inline
[ ] When hiding the HUD, also hide all the other elements, like settings and help buttons
[ ] Add an outer boundary, so that camera can't go where the cube won't be visible anymore because the distance is too big.